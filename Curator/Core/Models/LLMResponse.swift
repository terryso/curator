import Foundation

/// Response from an LLM analysis request.
///
/// Placeholder type for Story 1.2 — will be expanded in
/// Epic 2 (LLM Gateway) with structured response parsing.
struct LLMResponse: Sendable {
    let text: String
}
